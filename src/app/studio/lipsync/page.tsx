import { HonestPlaceholder } from "@/components/studio/HonestPlaceholder";

export default function StudioLipsyncPage() {
  return (
    <HonestPlaceholder
      title="Lipsync"
      body="Placeholder route — requires explicit adapter wiring and consent-aware workflows."
    />
  );
}
