import { HonestPlaceholder } from "@/components/studio/HonestPlaceholder";

export default function StudioEditPage() {
  return (
    <HonestPlaceholder
      title="Edit & repair"
      body="Repair loops and edit pipelines will land once manifests and adapters are defined. No fake integrations here."
    />
  );
}
