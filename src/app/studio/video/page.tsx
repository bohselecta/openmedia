import { HonestPlaceholder } from "@/components/studio/HonestPlaceholder";

export default function StudioVideoPage() {
  return (
    <HonestPlaceholder
      title="Video studio"
      body="OpenMediaForge MVP does not ship hosted video compute. This route is a labeled shell for future local or BYOK adapters."
    />
  );
}
