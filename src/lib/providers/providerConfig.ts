/**
 * Provider configuration surface types — canonical definitions live in
 * `contracts/provider-adapter.contract.ts`.
 */
export type {
  ComfyAdapterSettings,
  ComfyWorkflowTemplate,
  GenericHttpAdapterSettings,
  GenericHttpOutputType,
  GenericHttpPollingMode,
  GenericHttpResponseMapping,
  ProviderAuthMode,
  ProviderConfig,
} from "@contracts/provider-adapter.contract";
